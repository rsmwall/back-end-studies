#include <stdio.h>

int main(void) {
    // operacaoes matematicas em numeros
    // + adicao
    // - subtracao
    // * multiplicacao
    // / divisao
    // % mod

    int a = 5, b = 2;
    a + b;

    // operadores de atribuicao
    // = atribuicao simples
    // += adicao com atribuicao
    // -= subtracao com atribuicao
    // *= multiplicacao com atribuicao
    // /= divisao com atribuicao

    // var += var2; eh o mesmo que var = var + var2;
    int c = 3;
    c += a;

    return 0;
}