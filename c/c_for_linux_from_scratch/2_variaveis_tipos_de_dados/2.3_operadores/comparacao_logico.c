#include <stdio.h>

int main(void) {
    // comparam valores e retornam booleano
    // == igual a
    // > maior que
    // < menor que
    // >= maior ou igual que
    // <= menor ou igual que
    // != diferente de

    // operadores logicos
    // && E logico
    // || OU logico
    // ! NAO logico

    // operadores bit a bit
    // realizam operacoes a nivel de bit em numeros inteiros
    // & E bit a bit
    // | OU bit a bit
    // ^ OU <exclusivo> bit a bit
    // << deslocamento para esquerda
    // >> deslocamento para direita

    return 0;
}